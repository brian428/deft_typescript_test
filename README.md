# Ext JS and Deft JS TypeScript Test

A very basic, but working, demo of using TypeScript to create an Ext JS and Deft JS application. Uses TypeScript definitions for the Ext JS and Deft JS libraries.
